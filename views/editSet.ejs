<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit LEGO Set</title>
  <link rel="stylesheet" href="/css/main.css" />
</head>
<body>
  <%- include('partials/navbar', {page: '' }) %>
  <div class="container mx-auto p-4">
    <div class="hero bg-base-200 rounded-lg p-6">
      <div class="hero-content text-center">
        <div class="max-w-md">
          <h1 class="text-5xl font-bold">Edit Set</h1>
          <p class="py-6">Update the details of the LEGO set.</p>
          <form action="/lego/editSet/<%= set.set_num %>" method="POST">
            <div class="form-control">
              <label class="label">
                <span class="label-text">Set Name</span>
              </label>
              <input type="text" value="<%= set.name %>" name="name" class="input input-bordered" required>
            </div>
            <div class="form-control">
              <label class="label">
                <span class="label-text">Set Number</span>
              </label>
              <input type="text" value="<%= set.set_num %>" name="set_num" class="input input-bordered" readonly>
            </div>
            <div class="form-control">
              <label class="label">
                <span class="label-text">Year of Release</span>
              </label>
              <input type="number" value="<%= set.year %>" name="year" class="input input-bordered" required>
            </div>
            <div class="form-control">
              <label class="label">
                <span class="label-text">Number of Parts</span>
              </label>
              <input type="number" value="<%= set.num_parts %>" name="num_parts" class="input input-bordered">
            </div>
            <div class="form-control">
              <label class="label">
                <span class="label-text">Theme</span>
              </label>
              <select name="theme_id" class="select select-bordered">
                <% themes.forEach(theme => { %>
                  <option value="<%= theme.id %>" <%= theme.id == set.theme_id ? 'selected' : '' %>><%= theme.name %></option>
                <% }); %>
              </select>
            </div>
            <div class="form-control mt-6">
              <button type="submit" class="btn btn-primary">Update Set</button>
            </div>
            <div>
              <a href="/lego/deleteSet/<%= set.set_num %>" class="btn btn-error">Delete Set</a>
            </div>
          </form>
        </div>
      </div>
    </div>
  </div>
</body>
</html>
